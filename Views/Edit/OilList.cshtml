@model OilList;
@using WebApplication1.Models.Edit.ToRender;

@section Styles {
    <link rel="stylesheet" href="/css/Home/_Layout.css" />
    <link rel="stylesheet" href="/css/Home/Test.css" />
    <link rel="stylesheet" href="/css/Edit/CommonEditStyle.css" />
}

<div>
    <h3>Список</h3>
    <a href="EditOilList/?add=-1">
        <input class="inpBtn paddingUnit" type="button" name="add" value="Добавить" />
    </a>
    @foreach (var el in Model.MotorOils)
    {
        <div class="editUnit paddingUnit">
        <h4>Масло: </h4><h4>@el</h4>
        <a href="EditOilList?edit=@el.id">
            <input class="inpBtn" type="button" name="add" value="Редактировать" />
        </a>
        <a href="EditOilList?delete=@el.id">
            <input class="inpBtn" type="button" name="add" value="Удалить" />
        </a>
        </div>
    }
</div>
